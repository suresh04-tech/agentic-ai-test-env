[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    integration: requires a real PostgreSQL database (opt-in, see tests/integration/README.md)
filterwarnings =
    ignore::DeprecationWarning
